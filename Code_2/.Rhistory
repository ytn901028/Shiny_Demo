shiny::runApp()
library(data.table)
require(gridExtra)
require(maps)
runApp()
require(maps)
map_data('state')
head(all_states)
map_data("state")
require("maps")
map_data("state")
library(maps)
require("maps")
map_data("state")
